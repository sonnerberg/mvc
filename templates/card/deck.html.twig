{% extends "card/home.html.twig" %}
{% block body %}
	{{ parent() }}
	<pre>
		{{ dump() }}
	</pre>
	{{ deck }}
	<p>Amount of cards in deck:
		<span>{{ amount_cards }}</span>
	</p>
	<p>Card drawn:</p>
	<p>
		{{ cardsDrawn }}
	</p>
{% endblock %}
